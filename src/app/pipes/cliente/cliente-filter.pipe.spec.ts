import { ClienteFilterPipe } from './cliente-filter.pipe';

describe('ClienteFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClienteFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
