import { ServiciohUsuarioPipe } from './servicioh-usuario.pipe';

describe('ServiciohUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new ServiciohUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
