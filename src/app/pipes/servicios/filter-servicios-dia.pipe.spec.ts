import { FilterServiciosDiaPipe } from './filter-servicios-dia.pipe';

describe('FilterServiciosDiaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterServiciosDiaPipe();
    expect(pipe).toBeTruthy();
  });
});
