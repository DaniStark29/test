import { FilterRepartidorPipe } from './filter-repartidor.pipe';

describe('FilterRepartidorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRepartidorPipe();
    expect(pipe).toBeTruthy();
  });
});
