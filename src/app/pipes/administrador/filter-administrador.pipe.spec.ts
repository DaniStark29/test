import { FilterAdministradorPipe } from './filter-administrador.pipe';

describe('FilterAdministradorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAdministradorPipe();
    expect(pipe).toBeTruthy();
  });
});
